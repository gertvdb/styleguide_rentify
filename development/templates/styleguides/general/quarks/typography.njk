{# ----------------------------#}
{# ------Styleguide block------#}
{# ----------------------------#}
{% macro styleguide(DATA) %}
	<h1 class="styleguide__title js-scroll-nav__sub-title">Typography</h1>
	<small>/sass/base/_quarks/_typography.scss</small>

	<h4 class="styleguide__sub-title">Font stack</h4>
	<p class="styleguide__p">
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
	</p>
	<div>
		<ul>
            {% for font  in DATA.FONT_FAMILY %}
				<li><a href="/fonts/{{ font }}.zip" download>{{ font }}</a></li>
            {% endfor %}
		</ul>
	</div>

	<h4 class="styleguide__sub-title">Headings</h4>
	<p class="styleguide__p">
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
	</p>

	<small class="styleguide__info">Heading 1 - &lt;h1&gt; </small>

	{% macro h1(text) %}
		<h1>{{ text }}</h1>
    {% endmacro %}
    {% set h1 = h1('Good design is good business') %}
	<div class="styleguide__fonts">
    	{{ h1 }}
	</div>

	<small class="styleguide__info">Heading 2 - &lt;h2&gt;</small>

	{% macro h2(text) %}
		<h2>{{ text }}</h2>
    {% endmacro %}
    {% set h2 = h2('Good design is always obvious, while great design is always transparent') %}
	<div class="styleguide__fonts">
        {{ h2 }}
	</div>

	<small class="styleguide__info">Heading 3 - &lt;h3&gt;</small>

	{% macro h3(text) %}
		<h3>{{ text }}</h3>
    {% endmacro %}
    {% set h3 = h3('While everything is designed, only few things are designed to excell') %}
	<div class="styleguide__fonts">
        {{ h3 }}
	</div>


	<small class="styleguide__info">Heading 4 - &lt;h4&gt;</small>

    {% macro h4(text) %}
		<h4>{{ text }}</h4>
    {% endmacro %}
    {% set h4 = h4('Content is king and should always precede design. Design in the absence of content is not design, but decoration.') %}
	<div class="styleguide__fonts">
        {{ h4 }}
	</div>


	<small class="styleguide__info">Heading 5 - &lt;h5&gt;</small>
    {% macro h5(text) %}
		<h5>{{ text }}</h5>
    {% endmacro %}
    {% set h5 = h5('Online design trends change more often than the wind and slightly less often than my socks. Good design is timeless, good design creates culture. Culture shapes value and value determinates the future.') %}
	<div class="styleguide__fonts">
        {{ h5 }}
	</div>

	<small class="styleguide__info">Heading 6 - &lt;h6&gt;</small>
    {% macro h6(text) %}
		<h6>{{ text }}</h6>
    {% endmacro %}
    {% set h6 = h6('Always design a thing by considering it in its next larger context - a chair in a room, a room in a house, a house in an environment, an environment in a city plan.') %}
	<div class="styleguide__fonts">
        {{ h6 }}
	</div>

	<h4 class="styleguide__sub-title">Elements</h4>
	<p class="styleguide__p">
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
		Lorem ipsum dolor sit amet, consectetur adipiscing elit.
		Curabitur dignissim tristique lorem, quis pharetra tellus euismod eu.
	</p>

	<small class="styleguide__info">Paragraph - &lt;p&gt;</small>
    {% macro p(text) %}
		<p>{{ text }}</p>
    {% endmacro %}
    {% set p = p('Many desperate acts of design (e.g. gradients, drop shadows, and the gratuitous use of transparency) are perpetuated in the absence of a strong concept. Good ideas provide a framework for design, guiding the work.') %}
	<div class="styleguide__fonts">
        {{ p }}
	</div>

	<small class="styleguide__info">Small text - &lt;small&gt;</small>
    {% macro small(text) %}
		<small>{{ text }}</small>
    {% endmacro %}
    {% set small = small('People think that design is styling. Design is not style. It\'s not about giving shape to the shell and not giving a damn about the guts. Good design is a renaissance attitude that combines technology, cognitive science, human need, and beauty to produce something that the world didn\'t know it was missing.') %}
	<div class="styleguide__fonts">
        {{ small }}
	</div>

	<small class="styleguide__info">Blockquote - &lt;blockquote&gt; <br /> Author/source - &lt;cite&gt;</small>
    {% macro blockQuote(text, author) %}
		<blockquote>
			{{ text }}
			<cite>{{ author }}</cite>
		</blockquote>
	{% endmacro %}
    {% set blockQuote = blockQuote('Great design will not sell an inferior product, but it will enable great products to achieve their maximum potential.', 'Thomas J. Watson Jr.') %}
	<div class="styleguide__fonts">
        {{ blockQuote }}
	</div>

	<small class="styleguide__info">Hyperlinks - &lt;a href=""&gt;, &lt;span class="is-hyperlink"&gt;</small>
    {% macro a(text, href) %}
		<a href="{{ href }}" title="{{ text }}">{{ text }}</a>
    {% endmacro %}
    {% macro isHyperlink(text) %}
		<span class="is-hyperlink">{{ text }}</span>
    {% endmacro %}

    {% set a = a('This is a hyperlink', '#') %}
    {% set isHyperlink = isHyperlink('This looks like a hyperlink') %}

	<div class="styleguide__fonts">
        {{ a }}
        {{ isHyperlink }}
	</div>


	<small class="styleguide__info">Strong - &lt;strong&gt;</small>
    {% macro strong(text) %}
		<strong>{{ text }}</strong>
	{% endmacro %}

    {% set strong = strong('This text is strong') %}
	<div class="styleguide__fonts">
        {{ strong }}
	</div>

	<small class="styleguide__info">Emphasized - &lt;em&gt;</small>
    {% macro em(text) %}
		<em>{{ text }}</em>
    {% endmacro %}

    {% set em = em('This text is emphasized') %}
	<div class="styleguide__fonts">
        {{ em }}
	</div>

	<small class="styleguide__info">Quote - &lt;q&gt;</small>
    {% macro quote(text) %}
		<q>{{ text }}</q>
    {% endmacro %}

    {% set quote = quote('This text is quoted') %}
	<div class="styleguide__fonts">
        {{ quote }}
	</div>


	<small class="styleguide__info">Highlighted - &lt;mark&gt;</small>
    {% macro mark(text) %}
		<mark>{{ text }}</mark>
    {% endmacro %}

    {% set mark = mark('This text is highlighted') %}
	<div class="styleguide__fonts">
        {{ mark }}
	</div>

	<small class="styleguide__info">Obsolete/deleted/updated - &lt;del&gt;</small>
    {% macro del(text) %}
		<del>{{ text }}</del>
    {% endmacro %}

    {% set del = del('This text is obsolete/deleted/updated') %}
	<div class="styleguide__fonts">
        {{ del }}
	</div>
{% endmacro %}
